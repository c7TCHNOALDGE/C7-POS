# دليل تشغيل برنامج C7 للمبيعات والمحاسبة

## محتويات المجلد
| الملف | وظيفته |
|---|---|
| `index.html` | البرنامج كاملاً (نقطة البيع + لوحة الإدارة) |
| `manifest.json` و `sw.js` والأيقونات | تجعله يُثبَّت كتطبيق على الكمبيوتر والأندرويد ويفتح بدون إنترنت |
| `firestore.rules` | قواعد الأمان: تمنع الموظف من رؤية الحسابات أو تعديل الفواتير |
| `firebase.json` | إعداد الرفع على Firebase Hosting (اختياري) |

## التجربة السريعة (5 دقائق)
افتح `index.html` في متصفح Chrome، واختر **"جرّب البرنامج على هذا الجهاز فقط"**.
- الإدارة: `admin` / `1234`
- الموظف: `emp` / `1234`

البيانات في وضع التجربة تبقى على نفس الجهاز فقط.

## التشغيل الحقيقي من أي مكان (مرة واحدة)

### 1) قاعدة البيانات في Firebase
1. ادخل [console.firebase.google.com](https://console.firebase.google.com) وافتح مشروعك أو أنشئ مشروعاً جديداً.
2. **Build ← Authentication ← Get started ← Email/Password ← Enable**.
3. **Build ← Firestore Database ← Create database** (اختر أقرب منطقة، مثل `me-central`).
4. في Firestore افتح تبويب **Rules**، احذف الموجود، والصق محتوى ملف `firestore.rules` كاملاً، ثم **Publish**.
5. من **Project settings ← General ← Your apps** أضف تطبيق ويب (أيقونة `</>`) وانسخ كتلة `firebaseConfig`.

### 2) حساب المدير الأول
في **Authentication ← Users ← Add user** أنشئ مستخدماً بالبريد:
`admin@c7pos.app` وكلمة مرور قوية.
أول مرة تدخل بـ `admin` وكلمة المرور هذه، يصبح هذا الحساب هو المدير تلقائياً. بعدها تُنشئ حسابات الموظفين من داخل البرنامج (قسم الموظفين) ولا تحتاج الكونسول مرة أخرى.

### 3) ضع الإعدادات داخل البرنامج
افتح `index.html` بأي محرر نصوص، وابحث عن السطر:
```js
const FIREBASE_CONFIG = null;
```
واستبدله بالكتلة التي نسختها، مثال:
```js
const FIREBASE_CONFIG = { apiKey:"AIza...", authDomain:"c7-technology.firebaseapp.com", projectId:"c7-technology", appId:"1:..." };
```
(إذا لم تعدّله، سيطلب البرنامج لصق الإعدادات على كل جهاز عند أول فتح.)

### 4) ارفعه على رابط
أسهل طريقة: افتح [app.netlify.com/drop](https://app.netlify.com/drop) واسحب **المجلد كاملاً** إلى الصفحة. ستحصل على رابط `https://...netlify.app`.
بديل: Firebase Hosting عبر `firebase deploy` إذا كان لديك Firebase CLI.

### 5) ثبّته كتطبيق
- **أندرويد:** افتح الرابط في Chrome ← القائمة ⋮ ← **تثبيت التطبيق / إضافة إلى الشاشة الرئيسية**.
- **كمبيوتر:** افتح الرابط في Chrome أو Edge ← أيقونة التثبيت في شريط العنوان.
- لاحقاً يمكن تحويله إلى ملف APK لمتجر Google Play عبر [pwabuilder.com](https://www.pwabuilder.com) بإدخال الرابط.

## كيف يعمل من أي مكان بدون مشاكل
- كل الأجهزة تقرأ وتكتب في نفس قاعدة البيانات السحابية، والتغييرات تظهر فوراً (فاتورة يصدرها موظف تظهر عندك في لوحة الإدارة خلال ثوانٍ).
- عند انقطاع الإنترنت يستمر البيع، وتُحفظ العمليات على الجهاز ثم تُرفع تلقائياً عند عودة الاتصال. شارة "متصل / بدون إنترنت" أعلى الشاشة توضح الحالة.
- خصم المخزون وديون العملاء يتم بعمليات "زيادة/نقص" ذرّية، فلا تتضارب الأرقام إذا باع موظفان نفس الصنف في نفس اللحظة من فرعين مختلفين.
- الصلاحيات مفروضة من الخادم (قواعد الأمان) وليس من الواجهة فقط.

## ملاحظات مهمة
- أول دخول على أي جهاز جديد يحتاج إنترنت.
- نزّل **نسخة احتياطية** من الإعدادات أسبوعياً على الأقل.
- الخطة المجانية في Firebase تكفي نشاطاً صغيراً ومتوسطاً (حدود يومية للقراءة والكتابة). إذا كبر العمل كثيراً انتقل لخطة Blaze (الدفع حسب الاستخدام).
